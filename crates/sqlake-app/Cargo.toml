[package]
name = "sqlake-app"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
async-trait.workspace = true
serde_json.workspace = true
sqlake-core.workspace = true
thiserror.workspace = true
tokio.workspace = true
tracing.workspace = true
unicode-width.workspace = true

[dev-dependencies]
# Test-only. The fixtures exist to break naive formatting, so the formatter is
# run against them here rather than against values written to suit it.
sqlake-driver-mock.workspace = true
time = { workspace = true, features = ["macros"] }
tokio = { workspace = true, features = ["macros", "rt", "test-util"] }
